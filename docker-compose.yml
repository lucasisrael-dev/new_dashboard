services:
  app:
    build:
      context: .
      target: build-stage # Para usar o estágio de build no dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
